// Mute Insulters - final version :D
// made by /BudSpencer ( 2016 )


Mute_Insulters = [

	if ( || ( >= ( strstr $arg2 "motherfucker" ) 0 ) ( >= ( strstr $arg2 "mother fucker" ) 0 ) ) [
	
	say ( format "^f7You have been muted because you used ^f3offensive language^f7. Attention what you say on this ^f4server^f7." )
	mute $arg1 1
	
	]
	
]


addhandler "ontext" Mute_Insulters