// user config - for user system
// made by /BudSpencer ( 2016 )


relinquish_master_if_blocked = [ if ( = ( isinlist $BLOCKED-MASTERS ( getip $arg1 ) ) 1 ) [ setmaster $arg1 0 ; say "Your name is ^f3blocked^f7. You can't claim any ^f4privileges ^f7here." ] ]

addhandler "onsetmaster" relinquish_master_if_blocked
